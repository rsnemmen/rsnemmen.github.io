# CLAUDE.md

Jekyll-based academic portfolio on the al-folio theme, deployed to GitHub Pages.
Owned by Rodrigo Nemmen (astrophysicist/ML researcher, USP).

## Read this first

For build commands, Prettier rules (print width 150), pre-commit hooks, SCSS structure, naming conventions, front-matter standards, and the "never edit gh-pages" warning, see **AGENTS.md** at the repo root. That file is the source of truth for style/structure; this file only covers Claude-specific guidance.

## Quick start

- `docker compose pull && docker compose up` → http://localhost:8080
- Prettier enforced by pre-commit + CI; don't bypass it.
- ImageMagick must be installed locally for WebP generation during builds.

## Content source map

- About page content lives in `_pages/about.md`; its current profile image is configured there as `profile.jpg`.
- CV page routing/front matter lives in `_pages/cv.md`, but CV data is loaded from `assets/json/resume.json` via `_config.yml`.
- `_data/cv.yml` is only the fallback CV source if the JSON resume source is removed or disabled.
- Repository page data lives in `_data/repositories.yml`.

## Custom Jekyll plugins (`_plugins/`)

| Plugin                        | Purpose                                       |
| ----------------------------- | --------------------------------------------- |
| `external-posts.rb`           | Fetches posts from Bear blog RSS feeds        |
| `google-scholar-citations.rb` | Pulls citation counts                         |
| `inspirehep-citations.rb`     | Inspire HEP integration                       |
| `details.rb`                  | Collapsible `<details>` sections in Markdown  |
| `file-exists.rb`              | Liquid tag for checking optional file paths   |
| `hide-custom-bibtex.rb`       | Filters custom BibTeX fields before rendering |
| `remove-accents.rb`           | Liquid filter for accent-insensitive matching |

These repo-specific helpers are used in bibliography rendering, optional include hooks, and content cleanup, so prefer reusing them over adding duplicate Liquid logic.

## Recurring task: LLM ranking update

The `_pages/llm-ranking.md` page is updated regularly (roughly monthly).

- **Rendered page:** `_pages/llm-ranking.md` — update the "Updated on \<date\>" line near the top.
- **Source data (scratch, gitignored):** `_pages/coding.txt`, `_pages/general.txt` — personal scratch files where leaderboard data is pasted. **Never commit them.**
- **Figures:** `assets/img/clippies/general*.png`, `assets/img/clippies/coding*.png` — regenerated by an external notebook/script outside this repo. Do not try to regenerate them; ask the user if figures appear stale.
- Commit message convention: `updated rankings: ...` or `updated rankings with <model>`.

## Recurring task: citation updates

- `_data/citations.yml` is maintained by `bin/update_scholar_citations.py` and the scheduled `.github/workflows/update-citations.yml` workflow.
- Prefer updating the citation-fetching code or rerunning the automation over hand-editing `_data/citations.yml` unless the user explicitly asks for a manual data fix.

## Recurring task: publication updates

- Publications are rendered automatically from `_bibliography/papers.bib`; update that BibTeX file rather than editing the rendered publications page.
- Usual source is a NASA ADS BibTeX export of Rodrigo's publications, followed by local journal-name normalization before replacing `_bibliography/papers.bib`.
- Personal cleanup workflow, from the parent website directory: `~/Dropbox/codes/python/replacements.py ~/Dropbox/science/mypapers/references/journals.txt papers.bib "rsnemmen.github.io/_bibliography/papers.bib"`.
- Mark selected publications with `selected={true}` in the BibTeX entry.
- Useful display fields for selected papers include `abbr`, `html`, and `pdf`.

## Files and paths to leave alone

- `temp.md` at repo root — personal scratch, never commit, never edit unasked.
- `_pages/coding.txt`, `_pages/general.txt` — LLM-ranking scratch inputs, gitignored.
- `_site/`, `.jekyll-cache/`, `node_modules/` — build artifacts.
- `assets/img/**/*.webp` — auto-generated by `jekyll-imagemagick`; edit the source image, not the WebP.
- `gh-pages` branch — auto-generated by CI, never push to it directly.

## Deployment

`.github/workflows/deploy.yml` builds on push to `main`/`master`, pull requests, and manual dispatch, then deploys only for non-PR runs. It uses Ruby 3.3.5, Python 3.13, installs ImageMagick, sets `JEKYLL_ENV=production`, runs `bundle exec jekyll build`, and then PurgeCSS.

Other notable automation:

- `.github/workflows/update-citations.yml` updates `_data/citations.yml` on a schedule and commits changes when needed.
- `.github/workflows/prettier.yml`, `broken-links.yml`, and `axe.yml` cover formatting, link checking, and accessibility checks.
- `.github/workflows/lighthouse-badger.yml`, `deploy-image.yml`, `deploy-docker-tag.yml`, `docker-slim.yml`, and `codeql.yml` cover performance badges, container publishing, and code scanning.
